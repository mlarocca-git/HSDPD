% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.sdpd.model.R
\name{build_sdpd_model}
\alias{build_sdpd_model}
\title{Build an SDP-D Model Object}
\usage{
build_sdpd_model(
  endogenous = "var_y",
  lambda_0 = TRUE,
  lambda_1 = TRUE,
  lambda_2 = TRUE,
  covariates = 0,
  fixed_effects = TRUE,
  time_effects = FALSE,
  coeffs = NULL,
  check = TRUE,
  ww_index = NULL,
  ww_values = NULL,
  px_neighbors = NULL,
  rr_groups = NULL,
  sim = FALSE,
  pp = NULL,
  d_i = 100,
  sim_options = list(ww_type = "queen", px_neighbors = 8, index_group = rep(1, pp),
    fixed_effects = list(constant = FALSE, min = 10, max = 20), lambda_0 = list(constant
    = FALSE, min = -0.5, max = 0.5), lambda_1 = list(constant = FALSE, min = -0.5, max =
    0.5), lambda_2 = list(constant = FALSE, min = -0.5, max = 0.5), betas = list(constant
    = FALSE, min = c(0, 0, 0, 1), max = c(1, 1, 1, 4)), sigma_eps = list(constant = TRUE,
    min = 1, max = 1))
)
}
\arguments{
\item{endogenous}{Character scalar. Name of the endogenous variable.
Defaults to \code{"var_y"}.}

\item{lambda_0}{Logical scalar. Whether the first lambda component is active.
Defaults to \code{TRUE}.}

\item{lambda_1}{Logical scalar. Whether the second lambda component is active.
Defaults to \code{TRUE}.}

\item{lambda_2}{Logical scalar. Whether the third lambda component is active.
Defaults to \code{TRUE}.}

\item{covariates}{Character or numeric vector. Names or indices of exogenous
variables to include in the model. Use \code{0} for no covariates.
Defaults to \code{0}.}

\item{fixed_effects}{Logical scalar. Whether fixed effects are included.
Defaults to \code{TRUE}.}

\item{time_effects}{Logical scalar. Whether time effects are included.
Defaults to \code{FALSE}.}

\item{coeffs}{Optional numeric matrix or data frame. Coefficient matrix used
to infer available variable names and model components. If supplied, it must
have column names.}

\item{check}{Logical scalar. Whether model diagnostics should be computed
when enough information is available. Defaults to \code{TRUE}.}

\item{ww_index}{Optional object defining the index structure of the spatial
weight matrix.}

\item{ww_values}{Optional object defining the values of the spatial weight
matrix.}

\item{px_neighbors}{Optional object defining pixel or proximity neighbors.}

\item{rr_groups}{Optional matrix or data frame defining regional groups. If
\code{NULL} and \code{sim = TRUE}, groups are generated automatically.}

\item{sim}{Logical scalar. Whether the model should be simulated. Defaults to
\code{FALSE}.}

\item{pp}{Optional integer. Cross-sectional dimension of the model. Required
when \code{sim = TRUE}.}

\item{d_i}{Integer. Approximate group-size divisor used when groups are
generated automatically. Defaults to \code{100}.}

\item{sim_options}{List. Simulation options used when \code{sim = TRUE}.
Expected elements include \code{ww_type}, \code{px_neighbors}, \code{index_group},
\code{fixed_effects}, \code{lambda_0}, \code{lambda_1}, \code{lambda_2}, \code{betas}, and
\code{sigma_eps}.}
}
\value{
A list of classless model components representing an SDP-D model.
The returned object may contain the following elements:
\describe{
\item{lambda_coeffs}{Logical vector identifying active lambda components.}
\item{name_endogenous}{Name of the endogenous variable.}
\item{beta_coeffs}{Logical vector identifying active beta coefficients.}
\item{fixed_effects}{Logical value indicating whether fixed effects are included.}
\item{time_effects}{Logical value indicating whether time effects are included.}
\item{coeffs}{Coefficient matrix, when generated or supplied.}
\item{ww_index}{Spatial-weight index structure, when available.}
\item{ww_values}{Spatial-weight values, when available.}
\item{px_neighbors}{Pixel or proximity-neighbor information, when available.}
\item{sigma_eps}{Error-term variance parameter, when available.}
\item{groups}{Regional group structure, when available.}
\item{diagnostics}{Model diagnostics, when \code{check = TRUE} and diagnostics can be computed.}
}
}
\description{
Builds an \code{sdpd_model} object, which is the main input used by subsequent
estimation functions in the package.
}
\details{
The function defines the active spatial-dynamic components, endogenous and
exogenous variables, fixed effects, time effects, spatial weight structures,
and optional simulation settings. If \code{sim = TRUE}, the model components are
randomly generated using the parameters supplied through \code{sim_options}.

If \code{check = TRUE}, and the generated or supplied model contains coefficient
and spatial-weight information, the function also runs model diagnostics using
\code{check_sdpd_model()}.

At least one of \code{lambda_0}, \code{lambda_1}, or \code{lambda_2} must be active.

If \code{coeffs} is supplied, the function uses its column names to verify whether
requested covariates and fixed effects are available. Covariates not present
in \code{coeffs} are removed from the model with a warning.

When \code{sim = TRUE}, \code{pp} must be supplied. If \code{rr_groups} is not supplied, the
function automatically builds a group structure using \code{pp} and \code{d_i}.
}
\examples{
model <- build_sdpd_model(
  endogenous = "var_y",
  covariates = c("x_1", "x_2"),
  fixed_effects = TRUE,
  time_effects = FALSE,
  sim = FALSE
)

simulated_model <- build_sdpd_model(
  endogenous = "var_y",
  covariates = c("x_1", "x_2"),
  sim = TRUE,
  pp = 100
)

}
\seealso{
\code{\link[=generate_sdpd_model]{generate_sdpd_model()}}, \code{\link[=check_sdpd_model]{check_sdpd_model()}}
}
