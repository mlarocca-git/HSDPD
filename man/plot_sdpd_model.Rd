% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.sdpd.model.R
\name{plot_sdpd_model}
\alias{plot_sdpd_model}
\title{Plot an SDP-D Model Fit}
\usage{
plot_sdpd_model(
  res_fit,
  n_units = "all",
  n_vars = "all",
  which = 1,
  t_axis = list(t_labels = NULL, t_points = NULL),
  x_limit = NULL,
  y_limit = NULL,
  max_col = 5,
  point_col = 1
)
}
\arguments{
\item{res_fit}{Fitted SDP-D model result object.}

\item{n_units}{Integer or \code{"all"}. Number of spatial units to plot. Defaults
to \code{"all"}.}

\item{n_vars}{Integer or \code{"all"}. Number of variables to plot. Defaults to
\code{"all"}.}

\item{which}{Integer scalar. Plot type selector. Currently only \code{1} is
implemented. Defaults to \code{1}.}

\item{t_axis}{List. Time-axis options. Expected elements are \code{t_labels} and
\code{t_points}.}

\item{x_limit}{Optional numeric vector of length 2. X-axis limits.}

\item{y_limit}{Optional numeric vector of length 2. Y-axis limits.}

\item{max_col}{Integer. Maximum number of plot columns. Defaults to \code{5}.}

\item{point_col}{Color specification for plotted points or lines. Defaults
to \code{1}.}
}
\value{
The output of the selected plotting function, invisibly if that
function returns invisibly.
}
\description{
Dispatches plotting of an SDP-D fitted model object.
}
\details{
This function is a lightweight plotting wrapper. Currently, \code{which = 1}
dispatches to \code{\link[=plot_1_sdpd_model]{plot_1_sdpd_model()}}.

The renamed API uses snake_case argument names. The old \code{t.axis} structure is
now \code{t_axis}, with elements \code{t_labels} and \code{t_points}.
}
\examples{
\dontrun{
plot_sdpd_model(
  res_fit = fit,
  n_units = "all",
  n_vars = "all"
)
}
}
\seealso{
\code{\link[=plot_1_sdpd_model]{plot_1_sdpd_model()}}
}
