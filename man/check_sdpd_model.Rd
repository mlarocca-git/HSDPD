% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.sdpd.model.R
\name{check_sdpd_model}
\alias{check_sdpd_model}
\title{Check an SDP-D Model}
\usage{
check_sdpd_model(
  res_fit = NULL,
  model = NULL,
  ww_index = NULL,
  ww_values = NULL
)
}
\arguments{
\item{res_fit}{Optional fitted SDP-D model object. If supplied, coefficients
are taken from \code{res_fit$coeff_hat} and the model structure is taken from
\code{res_fit$model}.}

\item{model}{Optional SDP-D model object, typically created with
\code{\link[=build_sdpd_model]{build_sdpd_model()}}. Ignored if \code{res_fit} is supplied.}

\item{ww_index}{Optional matrix. Spatial-neighbor index matrix. If omitted,
it is taken from \code{model$ww_index}.}

\item{ww_values}{Optional matrix. Spatial-weight value matrix. If omitted,
it is taken from \code{model$ww_values}.}
}
\value{
A list containing:
\describe{
\item{diagnostics}{A data frame with eigenvalue-based diagnostics:
\code{max_mod_eigen_1}, \code{max_mod_eigen_2}, and \code{max_mod_eigen_a}.}
}

If the spatial matrix components are missing, a list with an \code{error} element
is returned.
}
\description{
Checks the validity of an SDP-D model or fitted SDP-D model by evaluating
stationarity-related diagnostics.
}
\details{
The function computes eigenvalue-based diagnostics for the reduced-form model
matrix using the coefficient matrix and the spatial weight matrix.

If \code{res_fit} is supplied, the function evaluates the model inherited from
\code{res_fit}; any separately supplied \code{model} object is ignored.

The function builds the spatial matrix using \code{\link[=build_spatial_matrix]{build_spatial_matrix()}} and
then computes diagnostics based on the lambda components available in the
coefficient matrix.
}
\examples{
\dontrun{
diagnostics <- check_sdpd_model(model = model)
}

}
\seealso{
\code{\link[=build_sdpd_model]{build_sdpd_model()}}, \code{\link[=build_spatial_matrix]{build_spatial_matrix()}}
}
