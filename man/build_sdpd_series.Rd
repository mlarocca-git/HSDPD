% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.sdpd.series.R
\name{build_sdpd_series}
\alias{build_sdpd_series}
\title{Build an SDP-D Series Object}
\usage{
build_sdpd_series(
  df_obj = NULL,
  px = NULL,
  rr_y = NULL,
  rr_xx = NULL,
  lon = NULL,
  lat = NULL,
  rr_groups = NULL,
  label_groups = NULL,
  model,
  check = TRUE,
  ww_index = NULL,
  ww_values = NULL,
  px_neighbors = NULL,
  sim = FALSE,
  nn = NULL,
  vec_options = list(px_core = 1, px_neighbors = 6, na_rm = TRUE, na_covs =
    "pairwise.complete.obs", covariates_sim_model = list(ar = c(0.8), sd = 1), markovian
    = TRUE, num_steps = 10)
)
}
\arguments{
\item{df_obj}{Optional list-like object. Used mainly to support parallelized
calls. If \code{px} is \code{NULL}, \code{px} is taken from \code{df_obj$px}.}

\item{px}{Optional object defining the spatial units or pixel structure.}

\item{rr_y}{Data frame, matrix, or \code{SpatRaster}. Endogenous variable series.
Required unless \code{sim = TRUE}.}

\item{rr_xx}{Optional data frame, matrix, or raster-like object containing
exogenous covariate series.}

\item{lon}{Optional vector or column name identifying longitude coordinates.}

\item{lat}{Optional vector or column name identifying latitude coordinates.}

\item{rr_groups}{Optional matrix or data frame defining regional groups.}

\item{label_groups}{Optional group labels, used mainly when reading raster
data.}

\item{model}{SDP-D model object, typically created with
\code{\link[=build_sdpd_model]{build_sdpd_model()}}.}

\item{check}{Logical scalar. Whether the resulting series should be checked
using \code{\link[=check_sdpd_series]{check_sdpd_series()}}. Defaults to \code{TRUE}.}

\item{ww_index}{Optional object defining the index structure of the spatial
weight matrix.}

\item{ww_values}{Optional object defining the values of the spatial weight
matrix.}

\item{px_neighbors}{Optional object defining pixel or proximity neighbors.}

\item{sim}{Logical scalar. Whether the data series should be simulated.
Defaults to \code{FALSE}.}

\item{nn}{Optional integer. Temporal dimension or number of observations used
when simulating the series.}

\item{vec_options}{List. Options used when reading or simulating vectorized
data. Expected elements include \code{px_core}, \code{px_neighbors}, \code{na_rm},
\code{na_covs}, \code{covariates_sim_model}, \code{markovian}, and \code{num_steps}.}
}
\value{
An SDP-D series object. The exact structure depends on the input data
source and on whether the object is read from observed data or simulated.
If \code{check = TRUE}, the returned object may also contain a \code{warnings}
element. If validation fails, a list with \code{errors} and \code{warnings} is
returned.
}
\description{
Builds an \code{sdpd_series} object, which is the data object expected by
subsequent estimation functions in the package.
}
\details{
The function reads observed data from a data frame, matrix, or raster object.
If \code{sim = TRUE}, the series is simulated using the model structure supplied
through \code{model} and the simulation settings supplied through \code{vec_options}.

If \code{sim = FALSE}, \code{rr_y} must be supplied.

If \code{sim = TRUE}, the function calls \code{\link[=generate_sdpd_series]{generate_sdpd_series()}} and uses the
spatial structure stored in \code{model}, including \code{ww_index}, \code{ww_values},
\code{px_neighbors}, and \code{groups}.

Data are read using \code{\link[=read_data_from_dataframe]{read_data_from_dataframe()}} when \code{rr_y} is a data frame
or matrix, and using \code{\link[=read_data_from_raster]{read_data_from_raster()}} when \code{rr_y} inherits from
class \code{SpatRaster}.
}
\examples{
series <- build_sdpd_series(
  px = px,
  rr_y = rr_y,
  rr_xx = rr_xx,
  model = model
)

simulated_series <- build_sdpd_series(
  model = model,
  sim = TRUE,
  nn = 50
)

}
\seealso{
\code{\link[=build_sdpd_model]{build_sdpd_model()}},
\code{\link[=generate_sdpd_series]{generate_sdpd_series()}},
\code{\link[=check_sdpd_series]{check_sdpd_series()}},
\code{\link[=read_data_from_dataframe]{read_data_from_dataframe()}},
\code{\link[=read_data_from_raster]{read_data_from_raster()}}
}
