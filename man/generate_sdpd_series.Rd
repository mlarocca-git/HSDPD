% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.sdpd.series.R
\name{generate_sdpd_series}
\alias{generate_sdpd_series}
\title{Generate an SDP-D Series}
\usage{
generate_sdpd_series(
  nn,
  rr_y,
  rr_xx,
  model,
  markovian,
  num_steps,
  covariates_sim_model
)
}
\arguments{
\item{nn}{Optional integer. Number of time observations to simulate. Required
when \code{rr_y} is \code{NULL}.}

\item{rr_y}{Optional numeric matrix. Initial endogenous series. If supplied
and \code{nn} is \code{NULL}, the number of time observations is inferred from
\code{ncol(rr_y)}.}

\item{rr_xx}{Optional numeric matrix, data frame, or three-dimensional array.
Exogenous regressor series. If \code{NULL} and the model has covariates,
regressors are simulated.}

\item{model}{SDP-D model object, typically created with
\code{\link[=build_sdpd_model]{build_sdpd_model()}}. It must contain coefficient and spatial-structure
components.}

\item{markovian}{Logical scalar. Whether to use the Markovian autoregressive
update in \code{\link[=fit_sdpd_series]{fit_sdpd_series()}}.}

\item{num_steps}{Integer. Number of iterative update steps used by
\code{\link[=fit_sdpd_series]{fit_sdpd_series()}}.}

\item{covariates_sim_model}{List. ARIMA model specification passed to
\code{\link[stats:arima.sim]{stats::arima.sim()}} when covariates are simulated.}
}
\value{
A list containing:
\describe{
\item{rr_y}{Simulated endogenous series.}
\item{rr_xx}{Observed or simulated exogenous regressors.}
\item{eps}{Innovation-error matrix.}
}

If required inputs are missing or incompatible, a list with an \code{error} element
is returned.
}
\description{
Simulates a multivariate or spatio-temporal time series from an SDP-D model.
}
\details{
The function generates the endogenous series, optional exogenous regressors,
and innovation errors using the coefficient and spatial-structure components
stored in an SDP-D model object.

If \code{rr_y} is not supplied, the function initializes it as a zero matrix with
dimensions inferred from \code{model$coeffs} and \code{nn}.

If the model contains active beta coefficients and \code{rr_xx} is not supplied,
covariates are simulated as stationary ARIMA processes using
\code{\link[stats:arima.sim]{stats::arima.sim()}}. A covariate named \code{"trend"} is generated as a normalized
deterministic time trend.
}
\examples{
simulated_data <- generate_sdpd_series(
  nn = 50,
  rr_y = NULL,
  rr_xx = NULL,
  model = model,
  markovian = TRUE,
  num_steps = 10,
  covariates_sim_model = list(ar = c(0.8), sd = 1)
)

}
\seealso{
\code{\link[=build_sdpd_model]{build_sdpd_model()}}, \code{\link[=check_sdpd_series]{check_sdpd_series()}},
\code{\link[=build_spatial_matrix]{build_spatial_matrix()}}, \code{\link[=fit_sdpd_series]{fit_sdpd_series()}}, \code{\link[stats:arima.sim]{stats::arima.sim()}}
}
