% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fun.plot.stat.discrete.RESIDs.R
\name{plot_stat_discrete_resids}
\alias{plot_stat_discrete_resids}
\title{Plot Spatial Residual Statistics}
\usage{
plot_stat_discrete_resids(
  results,
  statistic = mean,
  main = NULL,
  significant_test = FALSE,
  by_adjusted = FALSE,
  alpha = NULL,
  mid_value = 0,
  size_point = 1,
  ...
)
}
\arguments{
\item{results}{List or data frame. SDP-D estimation results containing
residuals, longitude coordinates, and latitude coordinates.}

\item{statistic}{Function. Summary function applied to residuals. Defaults to
\code{\link[=mean]{mean()}}.}

\item{main}{Optional character scalar. Plot title.}

\item{significant_test}{Logical scalar. Whether values should be converted
into significance classes using \code{alpha}. Defaults to \code{FALSE}.}

\item{by_adjusted}{Logical scalar. Whether p-values should be adjusted using
the Benjamini-Yekutieli method. Defaults to \code{FALSE}.}

\item{alpha}{Optional numeric scalar. Significance level used when
\code{significant_test = TRUE}.}

\item{mid_value}{Numeric scalar. Midpoint used for the diverging color scale.
Defaults to \code{0}.}

\item{size_point}{Numeric scalar. Point size used in the plot. Defaults to
\code{1}.}

\item{...}{Additional arguments passed to \code{statistic}.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a spatial point plot of residual summary statistics.
}
\details{
The function computes a user-defined statistic on model residuals and plots
the resulting value at each spatial location. It can also plot residual
diagnostic p-values as significant versus not significant.

If \code{results$resid} is a data frame, the statistic is computed row-wise after
dropping the first residual column. If \code{results$resid} is a list, the
statistic is applied to each list element. Otherwise, the function assumes a
simple vector-like residual object.

If \code{by_adjusted = TRUE}, values are adjusted using \code{\link[stats:p.adjust]{stats::p.adjust()}} with
\code{method = "BY"}.
}
\examples{
\dontrun{
plot_stat_discrete_resids(
  results = fit,
  statistic = mean,
  main = "Mean residuals"
)

plot_stat_discrete_resids(
  results = fit,
  statistic = fun_lb_test,
  significant_test = TRUE,
  by_adjusted = TRUE,
  alpha = 0.05
)
}
}
\seealso{
\code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}, \code{\link[stats:p.adjust]{stats::p.adjust()}}
}
