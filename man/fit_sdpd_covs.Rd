% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.sdpd.covs.R
\name{fit_sdpd_covs}
\alias{fit_sdpd_covs}
\title{Compute SDP-D Covariance Components}
\usage{
fit_sdpd_covs(
  series,
  x = NULL,
  kk = 0,
  px_neighbors = NULL,
  nn,
  pp,
  na_covs = "pairwise.complete.obs"
)
}
\arguments{
\item{series}{Numeric matrix. Endogenous data series with spatial units in
rows and time observations in columns.}

\item{x}{Optional numeric matrix or three-dimensional array. Regressor data.
If \code{kk = 1}, \code{x} is expected to be a matrix. If \code{kk > 1}, \code{x} is expected
to be a three-dimensional array indexed by covariate, spatial unit, and
time.}

\item{kk}{Integer. Number of covariates. Defaults to \code{0}.}

\item{px_neighbors}{Optional list. Pixel or proximity-neighbor information.
If supplied, expected elements are \code{series_boundary} and \code{index}.}

\item{nn}{Integer. Number of time observations.}

\item{pp}{Integer. Number of spatial units.}

\item{na_covs}{Character scalar. Missing-value handling method passed to
\code{\link[stats:cov]{stats::cov()}}. Defaults to \code{"pairwise.complete.obs"}.}
}
\value{
A list containing:
\describe{
\item{index}{Neighbor index matrix.}
\item{cov11}{Covariance matrix between \code{series} and the boundary-augmented
series.}
\item{cov12}{Lagged covariance matrix between \code{series} and the
boundary-augmented series.}
\item{cov_x}{Covariance component for regressors, or \code{NULL} when no
covariates are used.}
}
}
\description{
Computes covariance components used by the SDP-D coefficient-estimation
routine.
}
\details{
The function builds covariance matrices between the observed series, boundary
or neighbor-augmented series, and optional regressors. These covariance
components are then used by \code{\link[=fit_sdpd_coefficients]{fit_sdpd_coefficients()}}.

If \code{px_neighbors} is \code{NULL}, the function uses the original \code{series} as the
boundary series and constructs a full index matrix from the row names of
\code{series}.

If \code{px_neighbors} is supplied, the function appends
\code{px_neighbors$series_boundary} to the original series and uses
\code{px_neighbors$index} as the neighbor index matrix.
}
\examples{
covs <- fit_sdpd_covs(
  series = series,
  kk = 0,
  nn = ncol(series),
  pp = nrow(series)
)

covs <- fit_sdpd_covs(
  series = series,
  x = x,
  kk = 1,
  px_neighbors = px_neighbors,
  nn = ncol(series),
  pp = nrow(series)
)

}
\seealso{
\code{\link[=fit_sdpd_coefficients]{fit_sdpd_coefficients()}}, \code{\link[stats:cov]{stats::cov()}}
}
