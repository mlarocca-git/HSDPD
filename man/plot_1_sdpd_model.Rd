% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot1.sdpd.model.R
\name{plot_1_sdpd_model}
\alias{plot_1_sdpd_model}
\title{Plot Detailed SDP-D Model Diagnostics}
\usage{
plot_1_sdpd_model(
  res_fit,
  n_units = "all",
  n_vars = "all",
  t_axis = list(t_labels = NULL, t_points = NULL),
  x_limit = NULL,
  y_limit = NULL,
  max_col = 5,
  point_col = 1
)
}
\arguments{
\item{res_fit}{Fitted SDP-D model result object. Expected to contain \code{data},
\code{coeff_hat}, \code{resid}, and \code{model}.}

\item{n_units}{Integer, character vector, logical vector, or \code{"all"}.
Spatial units to plot. Defaults to \code{"all"}.}

\item{n_vars}{Integer, character vector, logical vector, or \code{"all"}.
Model components to plot. Defaults to \code{"all"}.}

\item{t_axis}{List. Time-axis options with elements \code{t_labels} and
\code{t_points}.}

\item{x_limit}{Optional numeric vector of length 2. X-axis limits for the
scatter plots.}

\item{y_limit}{Optional numeric vector of length 2. Y-axis limits for the
scatter plots.}

\item{max_col}{Integer. Maximum number of component columns shown per unit.
Defaults to \code{5}.}

\item{point_col}{Color specification for scatter-plot points. Defaults to
\code{1}.}
}
\value{
Invisibly returns \code{NULL}, or a list with an \code{error} element if invalid
input is detected.
}
\description{
Plots diagnostic panels for selected spatial units and selected model
components of a fitted SDP-D model.
}
\details{
For each selected spatial unit and model component, the function displays:
one scatter plot of the relevant signal against the observed series, and one
time-series plot comparing the observed series, component signal, residuals,
and optionally fixed effects.

The function assumes the renamed package API:
\itemize{
\item \code{res_fit$coeff_hat}, not \code{res_fit$coeff.hat};
\item \code{res_fit$data$ww}, not \code{res_fit$data$W};
\item \code{res_fit$data$xx}, not \code{res_fit$data$X};
\item lambda columns named \code{lambda_0}, \code{lambda_1}, and \code{lambda_2}.
}

The \code{fixed_effects} panel is excluded from the plotted panels because fixed
effects are represented as a horizontal dashed grey line in the time-series
panel.
}
\examples{
\dontrun{
plot_1_sdpd_model(
  res_fit = fit,
  n_units = "all",
  n_vars = "all"
)
}
}
