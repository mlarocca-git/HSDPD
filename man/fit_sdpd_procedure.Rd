% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.sdpd.procedure.R
\name{fit_sdpd_procedure}
\alias{fit_sdpd_procedure}
\title{Run the SDP-D Estimation Procedure}
\usage{
fit_sdpd_procedure(
  series,
  model,
  check = FALSE,
  two_stage = FALSE,
  na_covs = "pairwise.complete.obs"
)
}
\arguments{
\item{series}{SDP-D series object, typically created with
\code{\link[=build_sdpd_series]{build_sdpd_series()}}.}

\item{model}{SDP-D model object, typically created with
\code{\link[=build_sdpd_model]{build_sdpd_model()}}.}

\item{check}{Logical scalar. Whether stationarity diagnostics should be
computed for the estimated model. Defaults to \code{FALSE}.}

\item{two_stage}{Logical scalar. Whether the second-stage estimator should be
applied after the first-stage coefficient estimation. Defaults to \code{FALSE}.}

\item{na_covs}{Character scalar. Missing-value handling method passed to
covariance computations. Defaults to \code{"pairwise.complete.obs"}.}
}
\value{
A list containing estimation outputs, including spatial metadata,
coefficient estimates, fitted values, residuals, mean-equation summaries,
time effects, model object, warnings, and diagnostics. If validation fails,
a list with \code{errors} and \code{warnings} is returned.
}
\description{
Runs the full SDP-D estimation procedure on a single SDP-D series object.
}
\details{
The function checks the input series, computes covariance components, builds
the spatial weight matrix, estimates model coefficients, optionally applies
the second-stage estimator, computes fitted values and residuals, estimates
mean-equation summaries, and optionally checks stationarity diagnostics.

This function is the core single-series estimation routine used by
\code{\link[=fit_sdpd_model]{fit_sdpd_model()}}. For parallelized workflows, it is called separately on
each group-specific SDP-D series object.
}
\examples{
result <- fit_sdpd_procedure(
  series = series,
  model = model
)

result_checked <- fit_sdpd_procedure(
  series = series,
  model = model,
  check = TRUE,
  two_stage = TRUE
)

}
\seealso{
\code{\link[=fit_sdpd_model]{fit_sdpd_model()}},
\code{\link[=check_sdpd_series]{check_sdpd_series()}},
\code{\link[=fit_sdpd_covs]{fit_sdpd_covs()}},
\code{\link[=build_spatial_matrix]{build_spatial_matrix()}},
\code{\link[=fit_sdpd_coefficients]{fit_sdpd_coefficients()}},
\code{\link[=fit_second_stage]{fit_second_stage()}},
\code{\link[=fit_sdpd_series]{fit_sdpd_series()}},
\code{\link[=fit_sdpd_mean_equation_model]{fit_sdpd_mean_equation_model()}},
\code{\link[=check_sdpd_model]{check_sdpd_model()}}
}
