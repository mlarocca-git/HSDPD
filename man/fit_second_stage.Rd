% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.2nd.stage.R
\name{fit_second_stage}
\alias{fit_second_stage}
\title{Fit the Second-Stage SDP-D Estimator}
\usage{
fit_second_stage(data_series, x_centered, ww, model, coeff_hat, data = NULL)
}
\arguments{
\item{data_series}{Numeric matrix. Endogenous data series with spatial units
in rows and time observations in columns.}

\item{x_centered}{Numeric matrix or array. Mean-centered regressors. If the
model has more than one covariate, this is expected to be a
three-dimensional array indexed by covariate, spatial unit, and time.}

\item{ww}{Numeric matrix. Spatial weight matrix.}

\item{model}{SDP-D model object, typically created with
\code{\link[=build_sdpd_model]{build_sdpd_model()}}.}

\item{coeff_hat}{Numeric matrix. Current coefficient estimates to be updated.}

\item{data}{Optional list. Data summary object. Recomputed internally from
\code{data_series} and the active beta coefficients.}
}
\value{
A numeric matrix of updated coefficient estimates.
}
\description{
Fits the second-stage estimator for the beta coefficients and, when included
in the model, the fixed effects.
}
\details{
The function uses the dynamic spatial model components and an existing matrix
of coefficient estimates to update the beta coefficients through
location-specific least-squares regressions.

The function constructs the lambda components of the reduced-form model and
computes the transformed response used in the second-stage regression.

If \code{model$fixed_effects = TRUE}, the local regression includes an intercept
term and updates both beta coefficients and \code{fixed_effects}.
}
\examples{
\dontrun{
coeff_hat <- fit_second_stage(
  data_series = data_series,
  x_centered = x_centered,
  ww = ww,
  model = model,
  coeff_hat = coeff_hat
)
}
}
\seealso{
\code{\link[=build_sdpd_model]{build_sdpd_model()}}
}
