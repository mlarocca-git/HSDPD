% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.spatial.matrix.R
\name{build_spatial_matrix}
\alias{build_spatial_matrix}
\title{Build a Spatial Weight Matrix}
\usage{
build_spatial_matrix(ww_index, ww_values)
}
\arguments{
\item{ww_index}{Matrix. Spatial-neighbor index matrix. Each row identifies
the neighbors of the corresponding spatial unit. Positive entries are
interpreted as valid neighbor indices.}

\item{ww_values}{Matrix. Spatial-weight value matrix. Must have the same
structure as \code{ww_index}; values are assigned to the corresponding neighbor
positions in the output matrix.}
}
\value{
A square numeric matrix containing the spatial weights. The matrix
has dimension \code{pp} by \code{pp}, where \code{pp = nrow(ww_index)}. Row and column
names are inherited from \code{rownames(ww_index)}.
}
\description{
Builds the square spatial weight matrix \code{ww} from an index matrix and a
corresponding value matrix.
}
\details{
The function checks that the resulting spatial matrix:
\itemize{
\item contains no \code{NA} values;
\item has a zero diagonal;
\item has no rows with all zero entries.
}
}
\examples{
ww_index <- matrix(
  c(2, 3,
    1, 3,
    1, 2),
  nrow = 3,
  byrow = TRUE
)

ww_values <- matrix(
  c(0.5, 0.5,
    0.5, 0.5,
    0.5, 0.5),
  nrow = 3,
  byrow = TRUE
)

rownames(ww_index) <- colnames(ww_index) <- c("1", "2", "3")

build_spatial_matrix(ww_index, ww_values)

}
