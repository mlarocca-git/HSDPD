% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.sdpd.boot.series.R
\name{plot_sdpd_boot_series}
\alias{plot_sdpd_boot_series}
\title{Plot a Bootstrap SDP-D Series}
\usage{
plot_sdpd_boot_series(
  bb,
  pp,
  nn,
  eigen,
  yy_star_1,
  boot_options,
  res_fit,
  group_index
)
}
\arguments{
\item{bb}{Integer. Current bootstrap iteration.}

\item{pp}{Integer. Number of spatial units.}

\item{nn}{Integer. Number of time observations.}

\item{eigen}{Numeric vector. Eigenvalue diagnostics.}

\item{yy_star_1}{Numeric matrix. Bootstrapped time series.}

\item{boot_options}{List. Bootstrap options. Expected elements include
\code{y_limits}, \code{label_index}, and \code{folder}.}

\item{res_fit}{List. Fitted SDP-D model result containing data, coefficients,
and model information.}

\item{group_index}{Vector. Group index used to select the plotted spatial
unit.}
}
\value{
Invisibly returns the output file path of the generated JPEG plot.
}
\description{
Plots one bootstrapped SDP-D time series against the corresponding observed
series for diagnostic control.
}
\details{
The function randomly selects one spatial unit from the first bootstrap group
and saves a JPEG plot comparing the observed series and one bootstrapped
series. The plot title reports selected model diagnostics, lambda estimates,
neighborhood size, eigenvalue information, and bootstrap deviations.

If \code{boot_options$y_limits} is \code{NULL}, the y-axis limits are inferred from
\code{res_fit$data$series}.

The function assumes that \code{res_fit$data} contains \code{series}, \code{ww},
\code{px_neighbors}, and optionally \code{xx}. It also assumes that \code{res_fit$coeff_hat}
follows the renamed coefficient convention, with columns such as \code{lambda_0},
\code{lambda_1}, \code{lambda_2}, and \code{fixed_effects}.
}
\examples{
\dontrun{
plot_sdpd_boot_series(
  bb = 500,
  pp = pp,
  nn = nn,
  eigen = eigen_values,
  yy_star_1 = yy_star_1,
  boot_options = boot_options,
  res_fit = res_fit,
  group_index = group_index
)
}
}
