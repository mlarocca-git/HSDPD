% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test.sdpd.model.R
\name{test_sdpd_model}
\alias{test_sdpd_model}
\title{Test an SDP-D Model by Residual Bootstrap}
\usage{
test_sdpd_model(
  res_fit,
  px,
  model = NULL,
  n_boot = 399,
  h0 = c("zero", "constant", "grouped", "nospatial", "noautoregressive", "no_x",
    "constrained")[1],
  coeff_h0 = 0,
  group_index = rep(1, dim(res_fit$fitted)[1]),
  boot_options = list(markovian = FALSE, resid = c("fitted", "normal")[1], sigma_resid =
    1, boot_plot = FALSE, folder = "", y_limits = NULL, label_index = NULL)
)
}
\arguments{
\item{res_fit}{Fitted SDP-D model result object.}

\item{px}{Vector. Spatial-unit identifiers used in the fitted model.}

\item{model}{Optional SDP-D model object. If \code{NULL}, \code{res_fit$model} is used.}

\item{n_boot}{Integer. Number of bootstrap replications. Defaults to \code{399}.}

\item{h0}{Character or integer. Null hypothesis to test. Supported values are
\code{"zero"}, \code{"constant"}, \code{"grouped"}, \code{"nospatial"},
\code{"noautoregressive"}, \code{"no_x"}, and \code{"constrained"}.}

\item{coeff_h0}{Numeric scalar or matrix. Coefficient value under the null
when \code{h0 = "zero"}. Defaults to \code{0}.}

\item{group_index}{Vector. Group index used when \code{h0 = "grouped"}. Defaults
to one common group.}

\item{boot_options}{List. Bootstrap options. Expected elements are
\code{markovian}, \code{resid}, \code{sigma_resid}, \code{boot_plot}, \code{folder}, \code{y_limits},
and \code{label_index}.}
}
\value{
A list containing:
\describe{
\item{pvalue}{Bootstrap-test p-values.}
\item{n_boot}{Number of bootstrap replications.}
\item{h0}{Null hypothesis tested.}
\item{diagnostics_model}{Model diagnostics.}
\item{coeff_hat}{Estimated coefficients from the fitted model.}
\item{diagnostics_coeff_boot}{Bootstrap coefficient diagnostics.}
\item{diagnostics_sdevs_tsboot}{Bootstrap time-series deviation diagnostics.}
\item{warnings}{Model warnings, if available.}
}

If validation fails, a list with an \code{error} or \code{errors} element is returned.
}
\description{
Performs bootstrap-based hypothesis tests on fitted SDP-D model coefficients.
}
\details{
The function approximates the sampling distribution of estimated SDP-D
coefficients using residual bootstrap. It supports several null hypotheses,
including zero coefficients, constant coefficients, grouped coefficients, no
spatial effects, no autoregressive effects, no covariate effects, and selected
coefficient constraints.

The bootstrap can use either fitted residuals, \code{boot_options$resid = "fitted"}, or newly simulated normal residuals, \code{boot_options$resid = "normal"}.

The test uses a normal basic bootstrap approximation:
\deqn{
  p = 2 \Phi \left(-\left|\frac{\hat\theta - \theta_0}{s^*}\right|\right)
}
where \verb{s^*} is the bootstrap standard deviation.
}
\examples{
test_result <- test_sdpd_model(
  res_fit = fit,
  px = fit$px,
  h0 = "zero",
  n_boot = 399
)

}
\seealso{
\code{\link[=check_sdpd_model]{check_sdpd_model()}},
\code{\link[=fit_sdpd_series]{fit_sdpd_series()}},
\code{\link[=fit_sdpd_covs]{fit_sdpd_covs()}},
\code{\link[=fit_sdpd_coefficients]{fit_sdpd_coefficients()}},
\code{\link[=plot_sdpd_boot_series]{plot_sdpd_boot_series()}}
}
