% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.sdpd.coefficients.R
\name{fit_sdpd_coefficients}
\alias{fit_sdpd_coefficients}
\title{Estimate SDP-D Coefficients}
\usage{
fit_sdpd_coefficients(ww, covs, mu, model)
}
\arguments{
\item{ww}{Numeric matrix. Spatial weight matrix.}

\item{covs}{List. Covariance components used for coefficient estimation.
Expected elements are \code{index}, \code{cov12}, \code{cov11}, and, when covariates are
included, \code{cov_x}.}

\item{mu}{Numeric vector. Spatial-unit means used to compute fixed effects.}

\item{model}{SDP-D model object, typically created with
\code{\link[=build_sdpd_model]{build_sdpd_model()}}.}
}
\value{
A list containing:
\describe{
\item{coeff_hat}{Numeric matrix of estimated coefficients.}
}
}
\description{
Estimates the first-stage SDP-D model coefficients using spatial covariance
components and, when included in the model, computes fixed effects.
}
\details{
The function estimates active lambda coefficients and beta coefficients for
each spatial unit. If fixed effects are included in the model, they are
computed from the reduced-form coefficient matrix and the spatial-unit means.

Coefficients are estimated separately for each spatial unit through local
least-squares systems based on the covariance components.

If the local normal-equation matrix is not invertible, the corresponding
coefficient estimates are set to \code{NA}.
}
\examples{
coeffs <- fit_sdpd_coefficients(
  ww = ww,
  covs = covs,
  mu = mu,
  model = model
)

}
\seealso{
\code{\link[=build_sdpd_model]{build_sdpd_model()}}
}
