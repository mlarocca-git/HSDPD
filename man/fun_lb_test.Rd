% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fun.LBtest.R
\name{fun_lb_test}
\alias{fun_lb_test}
\title{Compute a Ljung-Box Test P-Value}
\usage{
fun_lb_test(x, ...)
}
\arguments{
\item{x}{Numeric vector. Input values to test for serial autocorrelation.}

\item{...}{Additional arguments. Currently unused.}
}
\value{
Numeric scalar. The Ljung-Box test p-value, or \code{NA_real_} if the test
cannot be computed.
}
\description{
Computes the p-value of the Ljung-Box test for serial autocorrelation in a
numeric vector.
}
\details{
The function is a safe wrapper around \code{\link[stats:Box.test]{stats::Box.test()}}. It returns
\code{NA_real_} when the input is too short, entirely missing, has zero variance,
or produces an error.

The test is computed using \code{type = "Ljung-Box"} and lag equal to
\code{min(12, floor(length(x) / 4))}.

Missing values are removed before computing the test.
}
\examples{
fun_lb_test(rnorm(100))

fun_lb_test(c(1, 2, 3, NA, 5, 6, 7, 8, 9, 10, 11))

}
\seealso{
\code{\link[stats:Box.test]{stats::Box.test()}}
}
