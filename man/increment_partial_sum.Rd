% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdpd.simulation.R
\name{increment_partial_sum}
\alias{increment_partial_sum}
\title{Increment SDP-D Simulation Partial Sums}
\usage{
increment_partial_sum(partial_sum, fit_results, simulated_coefficients)
}
\arguments{
\item{partial_sum}{List. Partial simulation accumulator containing
\code{estimation.bias}, \code{estimation.mse}, \code{resid.sim}, \code{contatore1}, and
\code{contatore2}.}

\item{fit_results}{Data frame or list. Fitted SDP-D results from
\code{\link[=fit_sdpd_model]{fit_sdpd_model()}} or a compatible object containing coefficient estimates
and residual diagnostics.}

\item{simulated_coefficients}{Matrix or data frame. True coefficients used in
the simulation.}
}
\value{
The updated \code{partial_sum} list.
}
\description{
Updates cumulative simulation summaries from one fitted SDP-D result.
}
\details{
The function preserves the legacy accumulator field names used by older
simulation scripts. Bias contributions are computed as estimated coefficient
minus simulated coefficient, and MSE contributions are the squared
differences. \code{contatore1} and \code{contatore2} are incremented only for
non-missing contributions.

Residual diagnostics are read from current names such as \code{mean_resid},
\code{sd_resid}, \code{pvalue_lb}, \code{pvalue_jb}, and \code{max_eigen_a}, while also accepting
legacy names such as \code{pvalue.LB}, \code{pvalue.JB}, and \code{max.eigenA}.
}
\examples{
partial_sum <- list(
  estimation.bias = data.frame(lambda_1 = c("1" = 0)),
  estimation.mse = data.frame(lambda_1 = c("1" = 0)),
  resid.sim = data.frame(mean = c("1" = 0)),
  contatore1 = data.frame(lambda_1 = c("1" = 0)),
  contatore2 = data.frame(mean = c("1" = 0))
)
fit_results <- data.frame(
  coeff_hat = data.frame(lambda_1 = c("1" = 0.3)),
  mean_resid = c("1" = 0.2)
)
simulated_coefficients <- data.frame(lambda_1 = c("1" = 0.1))

increment_partial_sum(partial_sum, fit_results, simulated_coefficients)

}
