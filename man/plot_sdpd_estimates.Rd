% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.sdpd.estimates.R
\name{plot_sdpd_estimates}
\alias{plot_sdpd_estimates}
\title{Plot Spatial SDP-D Estimates}
\usage{
plot_sdpd_estimates(
  results,
  item = "coeff_hat",
  main = NULL,
  sub = NULL,
  limits = NULL,
  mid = 0,
  size_point = 1
)
}
\arguments{
\item{results}{List or data frame. SDP-D estimation results containing
longitude coordinates, latitude coordinates, group information, and the
selected \code{item}.}

\item{item}{Character scalar. Name of the result component to plot. Defaults
to \code{"coeff_hat"}.}

\item{main}{Optional character scalar. Text prepended to each plot title.}

\item{sub}{Optional character scalar. Text appended to each plot title.}

\item{limits}{Optional numeric vector of length 2. Limits passed to
\code{\link[ggplot2:scale_colour_gradient2]{ggplot2::scale_colour_gradient2()}}.}

\item{mid}{Numeric scalar or vector. Midpoint value for the diverging color
scale. If scalar, it is recycled across all plotted columns. Defaults to
\code{0}.}

\item{size_point}{Numeric scalar. Point size used in the plot. Defaults to
\code{1}.}
}
\value{
A named list of \code{ggplot} objects, one for each plotted estimate.
}
\description{
Creates spatial point plots for one or more estimated SDP-D quantities.
}
\details{
The function extracts a matrix-like item from an SDP-D result object and
creates one \code{ggplot} object for each column. Each plot displays the selected
estimate at the corresponding longitude and latitude coordinates.

The function assumes that \code{results$lon}, \code{results$lat}, and
\code{results$group$LABEL} are aligned with the rows of \code{results[[item]]}.

For consistency with the renamed package API, the default \code{item} is
\code{"coeff_hat"}.
}
\examples{
plots <- plot_sdpd_estimates(
  results = fit,
  item = "coeff_hat",
  main = "Coefficient: "
)

plots$lambda_0

}
\seealso{
\code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}
}
