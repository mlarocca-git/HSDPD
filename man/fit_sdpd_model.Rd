% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.sdpd.model.R
\name{fit_sdpd_model}
\alias{fit_sdpd_model}
\title{Fit an SDP-D Model}
\usage{
fit_sdpd_model(
  series,
  model,
  check = FALSE,
  two_stage = FALSE,
  na_covs = "pairwise.complete.obs",
  parallelize = FALSE
)
}
\arguments{
\item{series}{SDP-D series object, typically created with
\code{\link[=build_sdpd_series]{build_sdpd_series()}}.}

\item{model}{SDP-D model object, typically created with
\code{\link[=build_sdpd_model]{build_sdpd_model()}}.}

\item{check}{Logical scalar. Whether model checks should be performed during
fitting. Defaults to \code{FALSE}.}

\item{two_stage}{Logical scalar. Whether the second-stage estimator should be
used. Defaults to \code{FALSE}.}

\item{na_covs}{Character scalar. Missing-value handling method passed to
covariance computations. Defaults to \code{"pairwise.complete.obs"}.}

\item{parallelize}{Logical scalar. Whether the series should be split into
parallelized spatial groups before estimation. Defaults to \code{FALSE}.}
}
\value{
A data frame or tibble containing assembled SDP-D estimation results.
}
\description{
Fits an SDP-D model to an SDP-D series object.
}
\details{
The function estimates H-SDPD model parameters either on the full series or,
when \code{parallelize = TRUE}, on parallelized groups of spatial units. Each
group-specific result is assembled into a standardized output and then row
bound into a single result object.

If \code{parallelize = TRUE}, the function first calls
\code{\link[=parallelize_sdpd_data]{parallelize_sdpd_data()}} to split the SDP-D series into group-specific
objects. Otherwise, the full \code{series} object is wrapped in a single-element
list.

Estimation is performed by \code{\link[=fit_sdpd_procedure]{fit_sdpd_procedure()}} and each group result is
assembled by \code{\link[=fit_sdpd_assemble]{fit_sdpd_assemble()}}.
}
\examples{
fit <- fit_sdpd_model(
  series = series,
  model = model
)

fit_parallel <- fit_sdpd_model(
  series = series,
  model = model,
  parallelize = TRUE
)

}
\seealso{
\code{\link[=build_sdpd_series]{build_sdpd_series()}},
\code{\link[=build_sdpd_model]{build_sdpd_model()}},
\code{\link[=fit_sdpd_procedure]{fit_sdpd_procedure()}},
\code{\link[=fit_sdpd_assemble]{fit_sdpd_assemble()}},
\code{\link[=parallelize_sdpd_data]{parallelize_sdpd_data()}}
}
