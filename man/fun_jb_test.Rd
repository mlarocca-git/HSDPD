% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fun.JBtest.R
\name{fun_jb_test}
\alias{fun_jb_test}
\title{Compute a Jarque-Bera Normality-Test P-Value}
\usage{
fun_jb_test(x, ...)
}
\arguments{
\item{x}{Numeric vector. Input values to test for normality.}

\item{...}{Additional arguments. Currently unused.}
}
\value{
Numeric scalar. The Jarque-Bera test p-value, or \code{NA_real_} if the
test cannot be computed.
}
\description{
Computes the p-value of the Jarque-Bera normality test for a numeric vector.
}
\details{
The function implements a simple Jarque-Bera test directly from sample
skewness and kurtosis. It returns \code{NA_real_} when the input is too short,
entirely missing, has zero variance, or produces an error.

The test statistic is computed as:
\deqn{
  JB = n \left(\frac{S^2}{6} + \frac{(K - 3)^2}{24}\right)
}
where \code{S} is sample skewness and \code{K} is sample kurtosis. Under the null
hypothesis of normality, the statistic is asymptotically distributed as a
chi-squared random variable with 2 degrees of freedom.
}
\examples{
fun_jb_test(rnorm(100))

fun_jb_test(c(1, 2, 3, NA, 5, 6, 7, 8))

}
\seealso{
\code{\link[stats:pchisq]{stats::pchisq()}}
}
