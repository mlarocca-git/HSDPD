% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallelize.sdpd.data.R
\name{parallelize_sdpd_data}
\alias{parallelize_sdpd_data}
\title{Parallelize SDP-D Series Data by Group}
\usage{
parallelize_sdpd_data(series_object, model)
}
\arguments{
\item{series_object}{SDP-D series object, typically created with
\code{\link[=build_sdpd_series]{build_sdpd_series()}}. It must contain \code{series}, \code{xx}, \code{px}, \code{lon}, \code{lat},
\code{group}, \code{ww_index}, \code{ww_values}, and \code{px_neighbors}.}

\item{model}{SDP-D model object, typically created with
\code{\link[=build_sdpd_model]{build_sdpd_model()}}.}
}
\value{
A list of group-specific SDP-D series objects. If there is only one
group, the original series object is returned inside a single-element list.
}
\description{
Splits an SDP-D series object into group-specific SDP-D series objects.
}
\details{
The function groups spatial units according to the \code{COD} column in
\code{series_object$group} and builds one SDP-D series object per group. It is used
by \code{\link[=fit_sdpd_model]{fit_sdpd_model()}} when \code{parallelize = TRUE}.

Parallelization is based on the group labels stored in
\code{series_object$group[, "COD"]}.

Each group-specific object is rebuilt with \code{\link[=build_sdpd_series]{build_sdpd_series()}} using the
full series, covariates, coordinates, spatial weights, and neighbor structure.
}
\examples{
\dontrun{
grouped_series <- parallelize_sdpd_data(
  series_object = series,
  model = model
)
}
}
\seealso{
\code{\link[=build_sdpd_series]{build_sdpd_series()}}, \code{\link[=fit_sdpd_model]{fit_sdpd_model()}}
}
