% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.sdpd.series.R
\name{fit_sdpd_series}
\alias{fit_sdpd_series}
\title{Compute Fitted Values and Residuals for an SDP-D Series}
\usage{
fit_sdpd_series(
  data_series,
  ww,
  x_centered = NULL,
  model,
  coeff_hat,
  time_effects = NULL,
  px_sim = NULL,
  resids = NULL,
  markovian = TRUE,
  num_steps = 10
)
}
\arguments{
\item{data_series}{Numeric matrix. Endogenous data series with spatial units
in rows and time observations in columns.}

\item{ww}{Numeric matrix. Spatial weight matrix.}

\item{x_centered}{Optional numeric matrix or three-dimensional array.
Mean-centered regressors. Required when the model has covariates.}

\item{model}{SDP-D model object, typically created with
\code{\link[=build_sdpd_model]{build_sdpd_model()}}.}

\item{coeff_hat}{Numeric matrix. Estimated coefficients.}

\item{time_effects}{Optional numeric vector of time effects.}

\item{px_sim}{Optional character or numeric vector identifying the spatial
units to update during simulation.}

\item{resids}{Optional numeric matrix of residuals. If supplied, the function
computes an iterative simulated/update path.}

\item{markovian}{Logical scalar. Whether to use the Markovian autoregressive
update when \code{resids} are supplied. Defaults to \code{TRUE}.}

\item{num_steps}{Integer. Number of iterative update steps when \code{resids} are
supplied. Defaults to \code{10}.}
}
\value{
A list containing:
\describe{
\item{series}{Possibly updated endogenous series.}
\item{fitted}{Fitted values.}
\item{resid}{Residuals.}
}
}
\description{
Computes fitted values and residuals for an SDP-D model, or simulates/update
a series using supplied residuals.
}
\details{
The function builds the active lambda, beta, fixed-effect, and time-effect
components of the SDP-D model. It then computes fitted values in the standard
case, or iteratively updates the series when residuals are supplied.

If \code{resids} is \code{NULL}, the function computes standard fitted values and
residuals from the supplied data series.

If \code{resids} is supplied and \code{markovian = TRUE}, the function uses a Markovian
autoregressive update based on the inverted contemporaneous spatial component.

If \code{resids} is supplied and \code{markovian = FALSE}, the function uses a
fixed-point autoregressive update.
}
\examples{
fitted_series <- fit_sdpd_series(
  data_series = data_series,
  ww = ww,
  x_centered = x_centered,
  model = model,
  coeff_hat = coeff_hat
)

simulated_series <- fit_sdpd_series(
  data_series = data_series,
  ww = ww,
  x_centered = x_centered,
  model = model,
  coeff_hat = coeff_hat,
  resids = resids,
  markovian = TRUE
)

}
\seealso{
\code{\link[=build_sdpd_model]{build_sdpd_model()}}
}
