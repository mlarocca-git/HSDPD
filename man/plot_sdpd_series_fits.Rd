% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.sdpd.series.FITs.R
\name{plot_sdpd_series_fits}
\alias{plot_sdpd_series_fits}
\title{Plot Fitted, Observed, and Residual SDP-D Series}
\usage{
plot_sdpd_series_fits(
  results,
  latitude,
  longitude,
  n_digits = 1,
  main = NULL,
  sub = NULL,
  xlab = NULL,
  ylab = NULL
)
}
\arguments{
\item{results}{List or data frame. SDP-D estimation results containing
\code{fitted}, \code{resid}, \code{lat}, and \code{lon}.}

\item{latitude}{Numeric scalar. Latitude of the location to plot.}

\item{longitude}{Numeric scalar. Longitude of the location to plot.}

\item{n_digits}{Integer. Number of digits used to round coordinates before
matching. Defaults to \code{1}.}

\item{main}{Optional character scalar. Plot title.}

\item{sub}{Optional character scalar. Plot subtitle.}

\item{xlab}{Optional character scalar. X-axis label.}

\item{ylab}{Optional character scalar. Y-axis label.}
}
\value{
A \code{ggplot} object, or a character message if the requested
coordinates are not present in the result object.
}
\description{
Plots fitted values, observed values, and residuals for the spatial location
closest to the supplied latitude and longitude after coordinate rounding.
}
\details{
The function extracts fitted values and residuals from an SDP-D result object,
reconstructs the observed series as \code{fitted + resid}, and displays the three
series in a single \code{ggplot}.

Residuals are shifted vertically by an offset so they can be displayed on the
same plot as the fitted and observed series. A secondary y-axis is added for
the residual scale.

Time labels are inferred from \code{colnames(results$fitted)}. Numeric time labels
are converted to dates using \code{\link[=as.Date]{as.Date()}}.
}
\examples{
plot_sdpd_series_fits(
  results = fit,
  latitude = 45.1,
  longitude = 9.2
)

}
\seealso{
\code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}
}
