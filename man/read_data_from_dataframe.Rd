% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.data.from.dataframe.R
\name{read_data_from_dataframe}
\alias{read_data_from_dataframe}
\title{Read SDP-D Data from a Data Frame or Matrix}
\usage{
read_data_from_dataframe(
  px,
  lat = NULL,
  lon = NULL,
  rr_y,
  rr_xx = NULL,
  rr_groups = NULL,
  model,
  ww_index,
  ww_values,
  px_neighbors
)
}
\arguments{
\item{px}{Optional vector. Spatial-unit identifiers to include. If \code{NULL},
all row names of \code{rr_y} are used.}

\item{lat}{Optional numeric vector or column index identifying latitudes.}

\item{lon}{Optional numeric vector or column index identifying longitudes.}

\item{rr_y}{Data frame or matrix. Endogenous series. Rows must identify
locations and columns must identify time points, except optional coordinate
columns named \code{latitude} and \code{longitude}.}

\item{rr_xx}{Optional list or array. Exogenous regressors.}

\item{rr_groups}{Optional data frame or matrix. Group information. Must
contain columns \code{COD} and \code{LABEL}.}

\item{model}{SDP-D model object, typically created with
\code{\link[=build_sdpd_model]{build_sdpd_model()}}.}

\item{ww_index}{Matrix. Spatial-neighbor index matrix.}

\item{ww_values}{Matrix. Spatial-weight value matrix.}

\item{px_neighbors}{List. Pixel or proximity-neighbor structure. Expected
elements are \code{index} and optionally \code{series_boundary}.}
}
\value{
A list containing:
\describe{
\item{series}{Endogenous series for selected pixels and required close
neighbors.}
\item{xx}{Optional regressor array.}
\item{ww_index}{Spatial-neighbor index matrix restricted to required
locations.}
\item{ww_values}{Spatial-weight value matrix restricted to required
locations.}
\item{px_neighbors}{Pixel-neighbor structure with \code{index} and
\code{series_boundary}.}
\item{px}{Selected spatial-unit identifiers.}
\item{lon}{Longitude vector, if available.}
\item{lat}{Latitude vector, if available.}
\item{group}{Group matrix with columns \code{COD} and \code{LABEL}.}
}

If input validation fails, a list with an \code{error} element is returned.
}
\description{
Extracts and organizes SDP-D input data from a data frame or matrix.
}
\details{
The function reads the endogenous series, optional coordinates, groups,
spatial-weight components, pixel-neighbor information, and optional
regressors. It also builds the boundary series needed for local spatial
estimation.

The function adds close spatial neighbors required by the spatial weight
matrix and builds a boundary series for farther neighbors found in
\code{px_neighbors$index}.

For consistency with the renamed package API, the returned regressor component
is named \code{xx}, not \code{X}, and the boundary component is named
\code{series_boundary}, not \code{seriesBoundary}.
}
\examples{
series_object <- read_data_from_dataframe(
  px = px,
  rr_y = rr_y,
  rr_xx = rr_xx,
  rr_groups = groups,
  model = model,
  ww_index = ww_index,
  ww_values = ww_values,
  px_neighbors = px_neighbors
)

}
\seealso{
\code{\link[=build_sdpd_series]{build_sdpd_series()}}
}
